spring:
  config:
  import: optional:file:.env[.properties]
  application:
    name: server
  jpa:
  hibernate:
  ddl-auto: update
  datasource:
    url: jdbc:mysql://monorail.proxy.rlwy.net:19427/railway
    username: root
    password: qajCKsTTlDGKxGZmLshsIUjirlukxtMV
    driver-class-name=com: com.mysql.cj.jdbc.Driver
  mail:
    host: ${SPRING_MAIL_HOST}
    port: ${SPRING_MAIL_PORT}
    username: ${SPRING_MAIL_USERNAME}
    password: ${SPRING_MAIL_PASSWORD}
    properties:
      mail:
        debug: false
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
server:
  address: 0.0.0.0
  port: 8080

#spring.jpa.database-platform = org.hibernate.dialect.MySQLDialect